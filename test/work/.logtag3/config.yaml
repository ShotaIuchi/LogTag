# Log columns display settings.
column:
    - name: TAG
      display: AAA
      enable: true
    - name: CATEGORY
      display: CATEGORY
      enable: true
    - name: FILE
      display: マルチばいと文字
      enable: true
    - name: LOG
      display: LOG
      enable: false

# Enable tag category.
# "<tag>-<subtag>"
category:
    # - "default"
    # - "android"
    # - "android-kernel"
    # - "etc..."
